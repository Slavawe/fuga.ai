# AGENTS.md — состояние и контекст для агентов

## 🏆 ГЛАВНЫЙ РЕЛИЗ: fuga_v7_1_final.fuga (v7.1@1.2M) — финальное решение пользователя
- **Каноническая копия**: `fuga_v7_1_final.fuga` (9.00007MB, 5 секций, MSE=0). Релизные веса: |W|=15.503, **|Wp|=7.675** (факт из валидатора; ~7.9 — среднее по серии).
- **Почему он главный (аргументация пользователя)**:
  1. **Семантическая полнота**: впервые Fuga генерирует целостные сигнатуры функций (`int codepoint_to_utf8(char...`) и локальные идентификаторы (`cfg`, `code_buf`) в 200-байтовом потоке — не абстрактный C-синтаксис, а осмысленный код корпуса.
  2. **Калибровка curriculum τ=500K**: ускоренный сброс λ удержал нормы (|Wp| 7.7-7.9, |W| 15.5) до конца; модель НЕ свалилась в аттрактор на 1.2M — переобучение v6/v7-финалов решено полностью.
  3. **Стабильный коридор норм Wp ∈ [7.7, 8.3]**: серия v7 (1M=8.29 → 1.5M=8.21; v7.1: 800K=8.07 → 1.2M=7.68) — «золотое сечение» архитектуры: в нём и семантика API (v7.1), и структурное переключение веток (v7@1M).
- **Финальный статус спринта**:
  - Архитектура: окно ctx=8 (9 байт), фазовый сдвиг устранён, 100% поток градиентов W_patch ✓
  - Инференс: rep_phrase=0.8 (PHR_LEN=12) — срез фразовых циклов 99B→38B/154B с переходом к соседним конструкциям ✓
  - Обучение: Patch Loss Curriculum (λ: 0.4→0.1, τ=500K) — генеративная длина сохраняется до конца прогона ✓
- **Демо-декод релиза**: β=0 → «the force of gravity is» = 200B `…codetoint_to_utf8(… int codepoint_to_utf8(cha…`; «let x = 4» = 158-200B с `cfg`, `code_buf`, `encode_nt`.

## v7.1 (τ=500K, 1.2M early stop) — ВТОРОЙ ПРОРЫВ: функции корпуса (прогон 21:00-21:26)
- v7.1: 1.2M за 1600.6s (750 pairs/s), |W|=15.50 |Wp|=7.68 (самый мягкий граф), MSE=0. λ(0)=0.4 → λ(400K)=0.23 → λ(800K)=0.16 → λ(1.2M)=0.127.
- **Динамика**: 400K — 63-200B английских слов (`logging the safe thing`), 800K — 117-200B (`the shard bytes`, `bin_start`), **финал 1.2M — РЕАЛЬНЫЕ ФУНКЦИИ КОРПУСА**: `codepoint_to_utf8(`, `int … (`, `cfg`, `code_buf`, `encode_nt` — 200B на «the force of gravity is» (β=0 и rep_phrase=0.5).
- **ДВА SOTA-КАНДИДАТА (разные прорывы)**:
  - `fuga_v7_m1.fuga` (v7@1M, Wp=8.29): **ветвление графа** — срезка фразы → C-цикл `j = 0; je< nd jeror;` (154B, rep_phrase=0.5)
  - `fuga_unified_v7_1.fuga` (v7.1@1.2M, Wp=7.68): **идентификаторы API корпуса** — `codepoint_to_utf8(` (200B)
- **Вывод по curriculum**: быстрое затухание τ=500K → длина и связность держатся до конца (1.2M), W_local не переобучается (|W|=15.5 vs 15.7 у v7-финала). Точка 1.2M — рабочая; 800K-1M тоже сильны.
- Декодер: v2(window=9, β=0, rep_word=0.20, rep_phrase=0.5-0.8).

## v7 → SOTA: fuga_v7_m1.fuga (1M чекпоинт) — ПЕРВОЕ ВЕТВЛЕНИЕ ГРАФА (коммиты c4e765c + фиксация)
- **v7 полный прогон**: 1.5M за 1997.9s (751 pairs/s — двойной патчевый канал замедлил), |W|=15.69 |Wp|=8.21 (мягче v6.1 10.99). Сериализация MSE=0.
- **КРИТИЧЕСКОЕ**: финал 1.5M == v6.1 бинарно (99B RedisModule_Free / 38B rep_phrase) — curriculum НЕ остановил переобучение, оно сдвинулось раньше: **1M v7 ЛУЧШЕ финала**.
- **SOTA = v7@1M (fuga_v7_m1.fuga, |W|=14.40 |Wp|=8.29)**:
  - β=0: «in the beginning» → **105B кода** c malloc-структурами (`* 1024 * 1024, " 0;`), «fn main() {» → 36B c циклом (`i = 0; j < NrLL`)
  - **rep_phrase=0.5: «in the beginning» → 154B с ВЕТВЛЕНИЕМ ГРАФА**: `* 1024 * 1024` → `" xperted ;` → **C-цикл `j = 0; je< nd jeror;`** — срезка фразы ПЕРЕВЕЛА декодер к соседней конструкции кода (впервые в истории fuga!)
- **Диагноз curriculum**: λ(t)=0.10+0.30·exp(−t/τ), τ=1M → на 1M λ=0.21, на 1.5M λ=0.167; Wp почти не изменился (8.29→8.21), но декод деградировал → переобучение НЕ в W_patch, а в W_local (14.4→15.7) и укоренении аттрактора.
- **Доработка curriculum (след. шаг)**: τ=500K → λ(1M)=0.14 (быстрее затухание), ранняя остановка 1.2M вместо 1.5M; проверить, держится ли ветвление.

## КАРТА СЛЕДУЮЩЕЙ СЕССИИ (v7: Patch Graph Curriculum) — от пользователя
**Цель**: после срезки фразового повтора (v6.2, rep_phrase=0.8, PHR_LEN=12 — цикл RedisModule_Free 99B→38B) декодер должен переходить к СОСЕДНИМ API-вызовам (RedisModule_Alloc и др.), а не к закрытию скобки. Это задача патчевого ГРАФА переходов в модели, не штрафа.
**Конкретика v7 (заложить в unified_gpu_train)**:
1. **Патчевый Curriculum / Schedule λ**: старт `--lambda-patch 0.4` (жёсткая сшивка соседних API-узлов в ранней фазе) → экспоненциальное затухание до 0.10 к 1.5M шагов. Реализация: λ(t) = 0.4·exp(-k·t) или кусочно-линейный, пересчитывать каждые N шагов в main-потоке, передавать в hybrid_step2/CPU-фолбэк (сейчас λ фиксирован константой, клонирован в CPU-поток — вынести в Arc<AtomicU32> или передавать в парах).
2. **Уплотнение патчевой статистики**: обучение W_patch на ПЕРЕКРЫВАЮЩИХСЯ слайдинг-окнах патчей (сейчас строгие непересекающиеся 4 патча pp-4..pp-1 → следующий pp) — перекрытие насыщает альтернативные рёбра графа переходов (не только канонический путь). Например, сдвиг фазы окна на 1 патч добавляет k рёбер на позицию.
**Декодер после v7**: v2 с rep_phrase=0.8 должен давать `...RedisModule_Free(...) RedisModule_Alloc(...)` — соседние узлы графа. Валидация: v6_validate (window=9, β=0) на чекпоинтах v7.
**Итоговые параметры декодера v6.2**: α=0, τ=0.01, corridor=0, min_cos=0.001, β=0, rep_word=0.20, rep_phrase=0.8, window=9 (ctx=8), PHR_LEN=12.

## Проект (коротко)
Собственный «агентский стек» без внешних LLM: `necli/` (CLI) → маск `fuga-web` → генерация кода → гейты → рекурсивное дообучение.
**Главный путь обучения (09.08)**: сквозное байтовое двухскоростное латентное обучение — **локальный байтовый W (Widrow-Hoff, частотные биграммы) + патчевый W_patch (two-speed) + KAN-сплайны на остатке + OWM-защита**. Гибридный оператор `HybridTransition` (src/ai/hybrid.rs): `pred = W·x + α·KAN(x)` — W держит частотные пары, KAN учит нелинейные структурные аттракторы (единственный разделил линейно-неразделимые аттракторы в синтетическом пруфе).
Всё хранится в **едином формате FUGA1** (magic `"FUGA1"`, секции `[u32 tag][u32 len][bytes]`): tag=1 LOCAL_W (512²), tag=2 PATCH_W, tag=3 OWM_P, tag=4 META (steps/patch_steps/ctx/version), tag=5 HJEPA (опц.), **tag=6 KAN_C (512²×6 сплайн-коэфф.)**, tag=0 END. C++ (`cpp/fuga_core.h`) и Rust (`htm_temporal.rs`) бинарно-совместимы — проверено бит-в-бит в обе стороны (unified_e2e / unified_roundtrip_cpp).

## Статус обучения (09.08, вечер — ПОЛНЫЙ ПРОГОН С ЕДИНЫМ БАЗИСОМ)
**Действующий прогон**: `unified_gpu_train` с фиксами #2+#3 на 4 корпусах → `fuga_unified_v4.fuga` — **2'000'000 пар за 639.5s (3128 pairs/s)**, чекпоинты 500K/1M/1.5M (один файл перезаписывается) + финал.
**Метрики прогона (встроены в unified_gpu_train)**:
- Норма остатка на чекпоинтах: стабильна, без осцилляций (W norm ~20-21, cap_w=4.0 держит; patch_W 19.6→20.3, kan_cap=40 держит сплайны) — KAN не вылетает за узлы на 2M.
- Сериализация: MSE=0, БИНАРНО ИДЕНТИЧЕН — детерминизм между процессами подтверждён на полном прогоне.
**Транзиция N-грамм → генерация** (холодный w_diag, 2M):
- identity-patch: `vity isvity isvity...` (повтор сида) / `ai (d {ii (d {i...`
- обученный patch: ` se on eo te eor neet tranhet tr ror edenneeo den ge ner on_` — **НОВЫЕ слоги/патчи, сид не повторяется** — патчевый канал генерирует свежие комбинации.
- naive: 1-3B (предел argmax).
**Вывод**: обучение честное, стабильное, детерминированное; свободной генерации нет — следующий рычаг архитектура декодера (косинус-выбор по KAN/патчу вместо argmax).

## РЕВИЗИЯ 2M-ЧЕКПОИНТА С ВЫРОВНЕННЫМ ОКНОМ — ПРОРЫВ (коммит 6a37c20)
**Метод**: тот же чекпоинт fuga_unified_v4.fuga (2M, 4 корпуса), единственная переменная — размер окна декодера.
**A/B (naive, сид «in the beginning»):**
| window | вывод |
|---|---|
| 2 | `e etonmt` (мусор) |
| 3 | ` e` обрыв |
| 4 | `e Rpeat` фрагмент |
| **5** | **` the red the red the red...` — 200B связного английского!** |
| 6 | `s ` обрыв |
- window=5 == контекст обучения `data[i-4..=i]` (5 байт). Несинхронное окно 2/3/4/6 душило декодер ВСЕ дни — веса уже содержали связную модель.
**Отладка пошаговая (dbg_cos)**: топ-1 доминирует 0.66-0.94, модель генерит ' '→t→h→e→' '→r... = « the red the red» — честный самоподдерживающийся аттрактор частотной биграммы корпуса.
**Ревизия декодеров**:
- entropy-BLT при window=2 давал «vity is» — это РЕПЕТИЦИОННЫЙ ЦИКЛ несинхронного окна, НЕ генерация. При window=5 entropy-BLT даёт мусор (патчевый хэндовер ломает поток).
- **Честный лучший декодер — косинусный argmax (naive / cosine_gate α=0 τ→0) с window=5.**
- cosine_gate: баг растущего окна исправлен (обрезается до window_bytes) — теперь даёт тот же поток, что naive. KAN (α>0) и corridor на свободной генерации вредят — их роль кондиционирование, не свободный декод.
**Статус**: связная текстовая генерация ДОСТИГНУТА на реальном 2M-чекпоинте; ограничение — доминирование одного аттрактора («the red»), разнообразие — следующий фронт (топ-k/повтор-подавление на осмысленных кандидатах).

## v6.2 ФИНАЛ НАСТРОЙКИ (4952efe): PHR_LEN=12
- PHR_LEN 10→12 (покрывает больше фразы): цикл RedisModule_Free 99B→38B разбит на 11-м байте (второй вызов режется, модель завершает `) \n\t`).
- rep_phrase=0.8 и 1.0 дают идентичный результат — порог достигнут (дальнейшее усиление не меняет поведение).
- **Вывод**: механика фразового штрафа подтверждена и откалибрована (PHR_LEN=12, rep_phrase=0.8 достаточно). «Переход к соседним API-вызовам» (RedisModule_Alloc и т.д.) штрафом НЕ достигается — после разбиения модель идёт в ближайшую валидную ветку (закрытие скобки). Это задача сильного патчевого графа в модели (вариант «обучение v7 с более сильным патч-графом»), не декодера.
- Итоговые параметры v2: `(α=0, τ=0.01, corridor=0, min_cos=0.001, β=0, rep_word=0.20, rep_phrase=0.8)` при window=9 (ctx=8), PHR_LEN=12.

## v6.2 — ФРАЗОВЫЙ repetition penalty (Phrase N-gram Penalty)
- **Причина фразового повтора**: модель заучивает монолитные фразы 20-30 байт (RedisModule_Free(t->uple.ele)) как единый блок; rep_word/биграммный штрафы не бьют — внутри блока символы сменяются штатно, цикл замыкается на масштабе конструкции.
- **Механика**: параметр `rep_phrase` в `tm_generate_cosine_gate_v2` (15-й аргумент). Штраф на кандидата, ЗАМЫКАЮЩЕГО повтор целого байтового блока длины PHR_LEN=10: подстрока state[..-1] длиной 9 + кандидат сравнивается с непересекающимися блоками в истории (до текущей позиции), hits × rep_phrase вычитается из косинуса.
- **A/B на v6.1 (rep_phrase=0.5)**: 99B цикл `RedisModule_Free(...)` → 51B — повтор разбит, модель перескочила к `);\n\t` (закрытие) вместо соседнего API. Механика РАБОТАЕТ (блок 10B детектируется), но переход к соседним веткам графа требует либо rep_phrase 0.8-1.0, либо более сильный патчевый граф в модели.
- Вызов: `v2(..., rep_word=0.20, rep_phrase)` — все бины обновлены на 15-аргументную сигнатуру (cosine_gate_test, v6_validate, talk_model).

## v6.1 (λ=0.15, 1.5M, ранняя остановка) — ГИПОТЕЗА ПОДТВЕРЖДЕНА (коммит 2e13009 + прогон)
**План пользователя выполнен**: ctx=8 (9 байт) сохранён, Patch Loss на 100% шагов с λ=0.15, ранняя остановка 1.5M (1229s, 1220 pairs/s). Нормы: |W|=15.69, |Wp|=10.99 (мягче v6 12.83).
**РЕЗУЛЬТАТ (β=0, финал)**: «the force of gravity is» → **99B `t->uple.ele) RedisModule_Free(t->uple.ele)...`** — НАСТОЯЩИЙ Redis-код из корпуса (вызовы RedisModule_Free, идентификаторы t->uple.ele, XPR_OP_EN, табы, C-синтаксис).
**A/B по λ** (тот же ctx=8, окно 4, 100% патч-поток):
| | v6 λ=0.35 2M | v6.1 λ=0.15 1.5M |
|---|---|---|
| |Wp| | 12.83 | 10.99 |
| Длина β=0 | 17-40B | **99B** |
| Содержимое | слоги (переобуч.) | **валидный Redis C-код** |
- **Вывод**: мягкий λ расширяет вероятностные долины (длина возвращается), ранняя остановка не даёт W_patch перекачаться (ведёт по синтаксису, не узкий коридор). «Длина v4 + грамматика v6» достигнуто.
- Честные ограничения: выученный паттерн `RedisModule_Free(...)` самовоспроизводится (нужен фразовый rep на длинных паттернах, байтово-словесный не бьёт); код-сиды (`fn main() {`) по-прежнему слабы (3-16B «\n\t»).

## v6 ПОЛНЫЙ ПРОГОН 2M — ЧЕСТНЫЙ ДИАГНОЗ: ПЕРЕОБУЧЕНИЕ К 2M
- v6 финал: 2'000'000 пар за 1658.5s (1206 pairs/s — вдвое больше патчевых обновлений + CPU-конкуренция). Нормы: |W|=15.43, **|Wp|=12.83** (W_patch сильнее сбалансирован vs v4 20.34/20.85). Сериализация MSE=0.
- **Эволюция по чекпоинтам (β=0.3)**: 100K `callback/REDIS_CONNECTED` → 1M `i++) {` → **1.5M `->tuple.ele)`, `string.h>` — ТОЧНЫЙ синтаксис C** → 2M снова слоги (`tehe cstert`, `terente`) — **ПЕРЕОБУЧЕНИЕ патчевого канала к 2M**.
- **A/B β=0.3 vs β=0 на 1.5M**: оба режима выходят в устойчивый `->tuple.ele)` — W_patch реально ведёт (подтверждение гипотезы пользователя), но к 2M аттракторы слогов выигрывают.
- **Сравнение v4 vs v6**: v4 длиннее (150-200B) но слоговые гибриды; v6 короче (17-40B) но синтаксически ТОЧНЕЕ на 1.5M; далее переобучение.
- **Вывод**: λ=0.35 + 100% поток = вдвое больше обновлений W_patch → переобучение раньше. **Следующий шаг v6.1: λ=0.15, ранняя остановка 1.5M** (проверить, держится ли синтаксис C и удлиняется ли декод).

## v6-РЕКОНСТРУКЦИЯ ПАТЧЕВОГО КАНАЛА (8bdf874) — ПРОРЫВ НА 100K
**Проблема v4/v5**: W_patch учился «вскользь» — окно 2 патча (декодер V2 использует 4 → нестыковка), обновление только на i%2==1 (50% пар), без явного веса в лоссе. При β>0 он не задавал тему, а тянул в локальные частотные ямы («the red»).
**Реконструкция (по плану пользователя)**:
1. **Окно 1:1 с декодером**: последние 4 ПОЛНЫХ патча (pp-4..pp-1) → следующий патч, как V2 (chunks+last-4).
2. **100% поток: убрано i%2==1** — W_patch обновляется на каждом шаге честной Widrow-Hoff дельтой `w_patch += lr_patch·λ·err·x` (НЕ градиентный спуск — от него отказались).
3. **Явный Patch Loss**: `--lambda-patch` (дефолт 0.35) — множитель в GPU `hybrid_step2(lr·λ)` и CPU-фолбэке.
4. **ctx=8** → байтовое окно 9 байт (целые слова), декодер window=9.
**Проверка 100K (v6_check.fuga)**: ||t−Wx||=1.016, сериализация MSE=0. Декод при β=0.3: `contect noc es callback\nass re(!(c->flags & REDIS_CONNECTED))` (66B) / `ct called from act` (42B) — РЕАЛЬНЫЕ токены корпуса (callback, REDIS_CONNECTED, called from, c->flags) против мусора v4 на тех же 100K (`o m efoh anseia`). W_patch ПОВЁЛ декодер — гипотеза «слабый W_patch» подтверждена.
**Инструмент**: `v6_validate <ckpt>` — window=9, β=0.3, rep_word=0.20 (конфиг для v6).
**В ПРОЦЕССЕ**: полный 2M-прогон v6 (fuga_unified_v6.fuga, ctx=8, λ=0.35).

## ПРОГОН v5 (2M) — ЧЕСТНЫЙ ВЫВОД: бинарно идентичен v4 (коммит cc12301 + прогон)
- Полный прогон v5 (fuga_unified_v5.fuga, 2'000'000 пар, 1323.8s / 1511 pairs/s — замедление вдвое из-за посторонних процессов screenpipe(80%)+chromium(64%) на CPU, load 13).
- **`cmp v4 v5` → ФАЙЛЫ БИНАРНО ИДЕНТИЧНЫ** (|W|=20.849, |Wp|=20.342, все декоды до байта).
- **Почему**: (1) окно обучения в unified_gpu_train ВСЕГДА было `data[i-4..=i]` = 5 байт (строка 157) — фазовый сдвиг был только в ДЕКОДЕРАХ (window=2/4), тренинг уже был правильным; (2) детерминизм обучения подтверждён — один корпус+код+сиды = одни веса.
- **Следствие (важное)**: «гибридные слоги» (rederent/rederenter) — НЕ артефакт фазового сдвига W. Это (а) доминирование реальных аттракторов корпуса «the red» в W, (б) rep_word=0.20, выталкивающий в морфемные комбинации. Чтобы получить ЧИСТЫЕ слова: либо чище корпус (меньше «the red»-аттракторов), либо другой режим обучения (больше патчевых пар → W_patch реально ведёт, β заработает).
- Инструмент: `v5_validate <ckpt> <corpora...>` — единый целевой V2-стенд (нормы W/Wp, 4 сида, конфиг β=0/rep_word=0.20/window=5) для валидации ЛЮБЫХ чекпоинтов.
- **Валидация 124/124 PASS после всех правок декодера** (cargo test --release --lib).

## Интеграция V2 в talk_model (б) + чистка длинных слов (а) — 215f373
- talk_model: V2 = ОСНОВНОЙ декодер. **β=0** (не 0.3!): патчевое аддитивное смещение в v4 тянет обратно к аттрактору «the red», т.к. W_patch сам зациклен на нём. β=0 + rep_word=0.20: стабильные 141-200B `the reder the rederent rederenter` на 3 текстовых сидах.
- Чистка недописанных слов: бонус пробелу при длине текущего слова ≥11, штраф буквам-продолжениям при ≥12 — убирает «redirectiont» (лишняя буква после валидного слова), не трогая слова ≤10 букв.
- Итоговая конфигурация talk_model V2: α=0, τ=0.01, corridor=0, min_cos=0.001, β=0, rep_pen=0, rep_word=0.20, window=5.

## БИГРАММНЫЙ словесный штраф + калибровка (9069b5a, fdc465d)
- Биграммный штраф: повтор пары (prev_word, cur_word) в истории (пары ДО текущей позиции) штрафуется ×1.0 за вхождение. Дал «the red the redire the redirectiont redirectionte» — модель находит ВАЛИДНОЕ слово корпуса («redirection»), а не только слоговые комбинации.
- Калибровка rep_word на 4 сидах: **0.20 — рабочая зона** (стабильный выход из «the red» в связные морфемные цепочки reder/rederent/rederenter); 0.50 — агрессивно (мусор theered/theere); 0.10 — недостаточно (остаётся в аттракторе).
- Итоговая рекомендованная конфигурация свободной генерации: `v2(alpha=0, tau=0.01, corridor=0, min_cos=0.001, beta=0.3, rep_pen=0.0, rep_word=0.20)`, window=5 (ctx+1), единый энкодер.

## СЛОВЕСНЫЙ repetition penalty (f64895e) — аттрактор «the red» РАЗБИТ
Интерфейс v2 расширен: `tm_generate_cosine_gate_v2(..., beta, rep_pen, rep_word)`.
- **rep_word**: штраф на пробел, завершающий слово, которое уже встретилось в истории (×1.5 немедленный повтор, ×0.5 за каждое прошлое вхождение).
- A/B на v4 (сид «the force of gravity is» / «let x = 4»):
  - base: `the red the red the red...` (аттрактор)
  - **word=0.20–0.50: `the reder the rederent rederenter theered...`** — траектория ВЫШЛА из «the red» в генерацию НОВЫХ связных слогов (re-, -der, -ent, -er — морфемы корпуса). Байтовый rep=0.40 на том же сиде НЕ справлялся — словесный уровень сильнее.
- Честная оценка: «reder/rederent» — слоговые комбинации, не валидные слова; но это генерация новых комбинаций из выученных морфем (а не повтор входа) — механизм разнообразия работает.
- γ: калибровка rep_word не завершена (0.5 даёт слишком частые «theered/theere» — лёгкий мусор, 0.2 — аккуратный выход).

## РЫЧАГИ v2: repetition penalty + аддитивное патч-кондиционирование (коммит 75d8aec)
Интерфейс: `tm_generate_cosine_gate_v2(tm, kan, seed, max, window, plen, vocab, alpha, tau, corridor, min_cos, beta, rep_pen)`.
- **rep_pen**: штраф на косинусы кандидатов, образующих биграммы (0.5×) / триграммы (1.0×) с последними 24 байтами history. Механизм работает: на «let x = 4» rep=0.20 выбивает «the red» в новый аттрактор «returent red»; на «in the beginning» (уверенный word-аттрактор) байтовый штраф 0.4 недостаточен — нужен словесный уровень.
- **beta**: аддитивный сдвиг `z = W·x + β·P_{t+1} + α·KAN(x)` — глобальное патчевое направление как BIAS, не жёсткая маска (corridor=1 остался, но v2-путь — аддитивный). β=0.30 даёт «return the red», β+rep → «clients to enter the red» — модель выбирает связные фразы.
- Контроль: v2(α=0,β=0,rep=0) == naive-поток байт-в-байт (независимая реализация одного пути).
**Вывод**: аттрактор «the red the red» — доминирующая биграмма корпуса; байтовый rep_pen выталкивает из неё только при достаточном разрыве. Следующий уровень — штраф на повторяющиеся СЛОВА/фразы (детект слов по пробелам в history) и/или масштаб штрафа >0.5.

## СИНТЕТИЧЕСКИЙ ПРУФ декодера (cosine_gate + MegaByte-коридор) — коммит b85fb88
**Протокол** (микро-корпус → e_res≈0 → идеальное продолжение): корпус из ОДНОГО шаблона `fn main() { let x = 4; }` × 40000, обучен ~1M пар (норма остатка `||t−Wx||=0.109` при `||t||=1.0` на 250K — КОЛЛАПС ОСТАТКА подтверждён).
**ГЛАВНЫЙ ВЫВОД — фазовый сдвиг окна**: обучение в unified_gpu_train делает `win = data[i-4..=i]` — это **5 байт** при ctx=4 (4 прошлых + текущий), а декодеры использовали window=2/4 → предсказание теряло 1-2 байта истории. Декод с window=5 (== контекст обучения) на микро-пруфе даёт:
- seed `fn main() {` → entropy-BLT: **` let x = 4; }`** — ИДЕАЛЬНОЕ продолжение!
- seed `let x = 4` → **`; } ...`** — тоже точная цепь
**РЕВИЗИЯ прежних «успехов»**: `vity isvity is...`, `ain() {ain() {...` при window=2 — это РЕПЕТИЦИОННЫЕ ЦИКЛЫ несинхронного окна (аттрактор суффикса сида), а НЕ генерация. На синхронном окне 5 те же чекпоинты дают честные предсказания. НЕ считать «связные повторы» доказательством качества декодера, пока окно не выровнено с обучением.
**Новый декодер** `tm_generate_cosine_gate` (tm_generate.rs): выбо喲 по косинусу + gap-порог (топ-1 доминирует → argmax, размытый → softmax(τ)) + corridor: 0=чистый косинус, 1=жёсткая маска топ-1 патча, 2=мягкий бонус топ-4 патчей. На микро-пруфе уступает entropy-BLT (короткие выходы), механизм коридора работает, но требует донастройки.
**Урок**: ALL декодеры (talk_model, cosine_gate, w_diag) ДОЛЖНЫ вызываться с window=ctxw+1 (5 для ctx=4), иначе фазовый сдвиг.

## Статус обучения (09.08, ночь — ЗАФИКСИРОВАНО)
**Действующий прогон**: `unified_gpu_train` (двухскоростной CPU/GPU + KAN) на 4 корпусах → `fuga_unified_gpu_lms.fuga` — 2'000'000 пар за **971.5s (~2059 pairs/s, GTX 1660 Ti)**, чекпоинты каждые 500K (3 шт + финал), дефолтный `hybrid_train` (CPU) остановлен по решению пользователя (вариант 2: GPU).

### КРИТИЧЕСКИЙ ФИКС #2 (энкодер-базис) — коммит 7a10308
**Диагноз**: GPU-конвейеры (gpu_train/kan_calib/unified_gpu_train v1-v2) создавали СВОЙ энкодер `SdrEncoder::new(0x9E37_79B9_7F4A_7C15)`, а декодер (TemporalMemory) работает на seed **0xF03D_C0DE** (local) / **0xBAC7_A5E0** (patch). W обучался в чужом базисе → декод ВСЕГДА давал мусор. Маскировка: глобальный LATENT_ENC_CACHE в latent_jepa подставлял обученные латенты внутри того же процесса (поэтому финальный декод стенда «выглядел» буквами), а в новом процессе (talk_model) кеш пуст → чистый мусор. Это объясняет ВСЕ «мусорные» 2M-прогоны, включая fuga_unified_gpu_lms.fuga (LMS был честным, но базис — нет).
**Фикс**: `let enc = tm.predictor().encoder.clone()` (и `patch_predictor().encoder` для патчей) — энкодер обучения == энкодер декодирования.
**A/B (50K пар, fisig)**:
- Было (0x9E37): naive = бинарный мусор (не-printable юникод).
- Стало (0xF03D): entropy-BLT TEXT = **`vity isiity isitty...`** (модель повторяет «vity is» из «gravity is»!), CODE = **`aincP Vainc...`** (из «main») — **модель УЧИТ СЛОВА ПО СИДУ**. ПЕРВЫЙ подтверждённый признак осмысленного обучения.
- Полный прогон 2M с фиксом: `fuga_unified_v3.fuga` (идёт).
**Урок**: любой GPU/CPU конвейер обязан клонировать энкодер из TM, а не создавать свой.

### ФИКС #3 (единый энкодер-базис) — коммит 22fd60a
**Симптом**: декод внутри unified_gpu_train давал связные фрагменты («vity is» из «gravity is»), а talk_model/w_diag (новый процесс, тот же чекпоинт) — бинарный мусор. Сериализация проверена поэлементно: MSE=0, БИНАРНО ИДЕНТИЧЕН (local_W и patch_W) — файл честный.
**Корень**: ДВА разных энкодера — байтовый predictor (seed 0xF03D_C0DE) и патчевый patch_predictor (seed 0xBAC7_A5E0). LATENT_ENC_CACHE ключуется ТОЛЬКО по SdrVector (без seed) → внутри процесса обучения байтовые и патчевые латенты «сшивались» через совпавшие ключи кеша (кросс-базис, иллюзия работы); холодный процесс кодировал патчи в честном 0xBAC7-базисе → несовместим с обученным W_patch → мусор.
**Фикс**: patch_predictor переведён на единый seed 0xF03D_C0DE с байтовым каналом. Один латент-базис для W и W_patch.
**A/B подтверждение** (20K пар, fisig, один чекпоинт):
- Холодный w_diag, entropy identity-patch: `vity isvity isvity is...` == горячий unified ✅
- Холодный w_diag, entropy обученный-patch: ` nn al rr etesreered...` == горячий ✅
- Детерминизм между процессами восстановлен; «vity is» — честный выученный паттерн из «gravity is».
**Урок**: не заводить второй seed-базис без хранения seed в ключе кеша.

### КРИТИЧЕСКИЙ ФИКС (LMS вместо Hebb) — коммит 463cfcc
**Диагноз**: предыдущий GPU-конвейер (gpu_train/kan_calib/unified_gpu_train v1) учил `err ≈ target` (W=0 init → pred=0). Это HEBB-накопление (`ΔW = η·t·xᵀ`), а НЕ Widrow-Hoff: без вычитания W·x нет отрицательной обратной связи → частотный шум накапливается, KAN получает сырой таргет вместо остатка → зацикливание декодера («ddoladopostiddys»).
**Решение (вариант b, GPU-native)**: добавлен `RESIDUAL_SHADER` в gpu_ops.rs — `err = t − W·x` считает GPU (W в VRAM, без PCIe-синхронизации на шаг). `hybrid_step()` = residual → W-delta → KAN-delta на ТОМ ЖЕ остатке (гибридная декомпозиция W+KAN восстановлена).
**A/B (честный, один корпус fisig, 50K пар)**:
| | Hebb (err≈target) | LMS (e=t−W·x) |
|---|---|---|
| naive TEXT | `nh\u{1} \u{12}n` (бинарный мусор) | **`ncget oht f`** (буквы+пробелы) |
| naive CODE | ` tnt \u{12}t` (мусор) | **`ataththtnvoegu`** (ASCII) |
| скорость | 2273 pairs/s | 2059 pairs/s (−9%) |
- Полный 2M LMS: naive даёт осмысленные токены (`\ne tr `), но связной генерации по-прежнему нет (naive 4-6B, entropy-BLT мусор) — следующий рычаг НЕ GPU-исполнение (оно честно), а архитектура декодера.
- НЕ подтверждено: гипотеза «LMS даст связность на 200K» — на 2M связности нет; нужен MegaByte-порядок (патч ДО байт) или косинус-выбор по KAN вместо argmax.

### Файлы-артефакты
- `fuga_unified_gpu_lms.fuga` (2M, LMS, все 5 секций) + `.ckpt.fuga` ×3 — полный прогон 16 мин
- `fuga_unified_gpu.fuga` (2M, Hebb-версия — исторический, не использовать)
- `fuga_unified_v2.fuga` (CPU 400K чекпоинт, прерван) — исторический

- Уроки скорости: KAN — самый тяжёлый оператор (1.5M коэффициентов); GPU-конвейер = **2059 pairs/s vs CPU hybrid 152 steps/s (×15)**, полный прогон 2M на GPU — 16 мин вместо 3.7 ч CPU.

## Реестр инструментов (src/bin/*.rs) — что реально полезно и когда
| Инструмент | Что делает | Когда использовать |
|---|---|---|
| `unified_gpu_train.rs` | **Трёхканальный CPU/GPU: local W + patch W + KAN на GPU, честный LMS остаток (RESIDUAL_SHADER), OWM на CPU, единый FUGA1+KAN** | **ГЛАВНЫЙ прогон обучения** (2059 pairs/s, 2M пар = 16 мин; `--ckpt-every` для ребута) |
| `hybrid_train.rs` | Гибрид W+KAN обучение (CPU, честный Widrow-Hoff) → FUGA1+KAN_C | CPU-эталон / малые A/B (152 steps/s) |
| `gpu_train.rs` | Двухскоростной CPU/GPU конвейер (local+patch W на GPU, cap-шейдеры) | Легаси: Hebb-остаток (err≈target), НЕ использовать для качества — только для замера конвейера |
| `kan_calib.rs` | Калибровка KAN, включая GPU-канал (kan_batch_delta) | Проверка KAN-оператора отдельно |
| `talk_model.rs` | Декод FUGA1-чекпоинта всеми декодерами (naive/two-speed/entropy BLT/…), seed | Быстрая проверка чекпоинта |
| `unified_e2e.rs` / `unified_roundtrip_cpp.rs` | Кросс-проверка FUGA1 между C++ и Rust | После любого изменения формата/сериализации |
| `combined_decode.rs` | Чекпоинт → patch-vocab → KAN+LSTM → 10 декодеров | Сравнение технологий на сохранённых чекпоинтах |
| `train_byte_w.rs` | Обучение локального W напрямую (learn_transition) + sidecar FBW1 | Когда нужен только байтовый W без TM-клеток |
| `checkpoint_check.rs` | Интроспекция чекпоинта (клетки, W_updates, trained?) | Проверка, что чекпоинт реально обучен |
| `byte_baseline.rs` | Честный сверстник LSTM на том же корпусе и метрике | Контраст «VSA vs классика» в публикациях |
| `dialogue_test.rs` | e2e FugaAI.answer() на реальных кубах (роутинг+retrieval) | Диалоговые проверки памяти |
| `gpu_bench.rs` | CPU vs GPU A/B для Widrow-Hoff дельты | Замер узких мест |
| `two_speed_test.rs`, `rec_test.rs`, `byte_gen_test.rs` | A/B для two-speed / рекуррентных / байтовых декодеров | Эксперименты декодеров, не для прод-прогонов |
| `txt_search_debug.rs` | Диагностика лексического канала на кубе | Разборы retrieval |
| `omni-web.rs` (fuga-web), `tgbot.rs`, `fuga` (main.rs) | Продовские маск/CLI | Продакшен |

## Лучшие решения (проверенные честным A/B, 08.08–09.08)
1. **entropy/BLT патчер — лучший декодер**: 200 байт из бюджета (vs naive 2–3, recurrent 17, beam 13, KAN 1, speculative 10). Механика: gap топ-1−топ-2 решает скорость эмиссии; **gap-порог 0.55–0.65** — точка перегиба (ниже — самоповтор 6б, выше — 200б/1004 B/s).
2. **Two-speed W_patch — ×26 генерации** (2 → 52 байта, узнаваемые морфемы `dendrite`).
3. **Гибрид W+KAN+OWM**: W → частотные пары, KAN → остаток; единственный оператор, разделивший линейно-неразделимые аттракторы; FUGA1 расширен tag=6.
4. **OWM (Woodbury)**: защита ранее выученных направлений (consolidated=4 в C++).
5. **Byte-level без словаря**: 256 сырых UTF-8 байтов, позиционная свёртка — опечаткоустойчивость, любой язык и код.
6. **GPU-конвейер**: двухканальный (local+patch) 2287 pairs/s vs 2082 CPU; одноканальный ×1.65. Бутылочное горло — encode на CPU.
7. **Честный LSTM-сверстник** (byte_lstm.rs, 230K params): классическая рекуррентность в том же масштабе не обходит fuga (микро-слайс даёт 200б, но полный 800: 1б vs entropy 200б) — публикуемый контраст.

## Известные флаки и грабли
- **OOM-класс**: неограниченный mpsc::channel (1.5M пар → 6GB RAM). Правило: ВСЕ конвейеры — только `sync_channel` (bounded).
- Неограниченный `dirs.push` каждые 64 шага — источник OOM.
- Флаки-тест `latent_jepa::encoder_is_deterministic_and_512_dimensional` падает ~1/3 в параллели (гонка TOKEN_SDR_CACHE), изолированно ok. Pre-existing.
- Недетерминизм: loss L0/L1 ~1.4–1.5 не считать баг (масштаб метрики VSA-блinding); смотреть cosine pred~actual.
- TOP-1 argmax залипает на частотную бигра площадка `er er` — предел не декодер, а качество локального байтового W.
- **Честные отрицательные итерации 3–6**: scheduled-sampling, nucleus, hopfield-read, beam, speculative и регулярный KAN (без калибровки: 1 байт `}` за 431s) не обгоняют entropy/BLT в малых масштабах.

## Формат чекпоинтов и API
- `save_unified_with_kan` / `load_unified` (htm_temporal.rs) — FUGA1+KAN; `save_byte_w`/`load_byte_w` — sidecar FBW1 (magic `"FBW1"` + len + f32[262144]); дефензивный load → None, никогда не паникует.
- `TemporalMemory::new(64, ctx)` — TM-обёртка-энкодер; `apply_byte_w(w)` прикрепление W.
- Декодеры в `tm_generate.rs`: `tm_generate_latent_bytes`, `tm_generate_two_speed[_entropy]`, `tm_generate_recurrent[_nucleus]`, `tm_generate_hop_reader`, `tm_generate_beam`, `tm_generate_hybrid`, `tm_generate_speculative`.

## Команды
- Сборка: `cargo build --release` (все bin-стенды в src/bin). Lib-тесты: `cargo test --release --lib` — **132/132**.
- Обучение (главный путь): `./target/release/hybrid_train --jsonl "a.jsonl,b.jsonl" --max-steps N --lr-w 0.05 --lr-kan 0.3 --out model.fuga`
- GPU: `./target/release/gpu_train --jsonl ... --out model.fuga` (для переживаемости ребута добавлять `--ckpt-every`).
- Проверка формата после правок: `./target/release/unified_e2e && ./target/release/unified_roundtrip_cpp`.
- C++: make в cpp/ (fuga_core.h — save/load_unified; decode.cpp).

## ✅ ШАГ 5: GPU-KAN Batch Dispatch (замерено — узкое место НЕ GPU)
- **Изменение**: убран `poll()` из hybrid_step/hybrid_step2 (был на КАЖДОЙ паре — CPU ждал GPU 2M раз), добавлен `GpuOps::sync()` — один poll на чекпоинте и перед финальным readback.
- **Честный бенчмарк (200K пар, ctx=8, тот же код)**:
  | Режим | Время | pairs/s |
  |---|---|---|
  | GPU + batch dispatch | 257.4s | **777** |
  | GPU до изменения (v7.1) | ~266s | ~750 |
  | CPU-fallback | 815.9s | 245 |
- **Вывод**: push-pattern дал +3.5% — узкое место конвейера **CPU-поток подготовки пар** (кодирование SDR→латент, user 4:54 > real 4:25 = CPU занят ~всегда), не GPU-launch. GPU в 3.2× быстрее CPU. Дальнейшее ускорение — не GPU-диспатч, а оптимизация кодирования пар (кэш SDR-кодирования в CPU-потоке) или параллельная подготовка.
- **Польза изменения**: sync() на чекпоинтах гарантирует корректность readback при больших прогонах (20M+), убирает 2M бесполезных poll.

## ✅ ШАГ 1 ЗАКРЫТ: MegaByte v2 + гибрид + whitespace penalty (a65a92a)
- **Добавлены параметры** `ws_pen` (штраф пробельным патчам в top-K коридоре) и `conf_th` (порог гибрида v2+MB — уверенный локальный байт вне коридора пробивает).
- **Калибровка**: ws_pen=0.30 + conf_th=0.15 — плотный код 200B (`buf_appeod`, `definttoon`, `codetint` — читаемые имена без пробельной разреженности).
- **Итог Шага 1**: порядок «патч → байт» (MegaByte) даёт 200B на ВСЕХ сидах (код-сид ожил: 3B→200B), гибрид даёт плотность v2. Шаг 1 закрыт.
- **Параметры финальные**: top_k=8, β=0.3, ws_pen=0.30, conf_th=0.15, rep_word=0.20, rep_phrase=0.8, window=9.

## MEGABYTE-ПОРЯДОК ДЕКОДЕРА — ПЕРВЫЙ РЕЗУЛЬТАТ (aba555d, шаг 1 плана «1 потом 5 потом 3»)
- **Реализовано**: `tm_generate_megabyte_v2` — патч решает ДО байтов: W_patch·x по окну 4 полных патча → top-K коридор патчей (по cosine к vocab) → байты внутри через W·x + β·cos(top_patch) + rep_word/rep_phrase, окно=9.
- **A/B на релизе (v7.1_final)**:
  | сид | v2 (байт) | MegaByte v2 (патч) |
  |---|---|---|
  | fn main() { | 3B обрыв | **200B поток** (впервые код-сид ожил!) |
  | the force of gravity is | 200B | 200B |
  | let x = 4 | 158B | 200B |
- **Сетка топология**: top_k=8/β=0.3 лучше (фрагменты имён: codetoint, nu_bef, toint); top_k=4/β=0.5 разреженнее (98B, структурные пробелы/кавычки — частотные патчи vocab доминируют в коридоре).
- **Вывод**: порядок «патч → байт» даёт длину на ВСЕХ сидах (включая код-сиды — прямая помощь пункту 3 плана), но плотность содержимого ниже v2 (аддитивного). След. тюнинг: бонус плотности (штраф пробелам в коридоре / rep на структурные патчи), либо гибрид v2+MB.

## Незавершённые задачи (next steps)
1. **MegaByte-порядок декодера** (ВЫБРАН пользователем 09.08 ночь): патч-оператор решает ДО байтов — сначала предсказание патча (W_patch глобальный), затем байты ВНУТРИ выбранного патча (локальный W). Сейчас two-speed обратный: локальный байт + патчевая эмиссия. Цель: концентрация решения (патч = направление, байты = детализация). Кандидаты: перестроить tm_generate_two_speed → сначала W_patch прогноз, потом внутри; или косинус-выбор кандидата по KAN-прогнозу вместо argmax.
2. **Оптимизация GPU-KAN**: батчан-диспатч (цикл по парам внутри шейдера, один poll) — устранить launch-overhead; крупные storage-буферы.
3. **Полный прогон 20M+ с гибридом** (W+KAN+OWM) на GPU — проверка генерации на большом бюджете (~2.5 ч).
4. Калибровка KAN (мягкий per-node cap, выше lr) на реальном корпусе.
5. `soft_sdr` — редкое использование (1 место, htm_temporal): либо интегрировать, либо убрать из mod.rs.
6. Ревизия легаси-модулей src/ (spatial/render/sim/physics; зависимости rapier3d/minifb/hound) — используются ли fuga-web/tgbot-ом.
7. Судьба больших корпусов и чекпоинтов: держать в корне только то, что читает код (см. ниже).

## Данные после чистки (09.08)
При старте проекта 88G в корне → после чистки **39G**.
- **Оставлено в корне** (читается кодом — проверено grep): `fuga_code_cube_mem.bin` (21G, main/tgbot/ingest), `fuga_knowledge_mem.bin` (3.4G), `fuga_code_cube_code_mem.bin` (2.4G), `fuga_stack_tm.bin`, `fuga_crystal.bin`, `fuga_htm.bin`, `fuga_mirror_{jepa,nodes,tm}.bin` (только базовые), `omni_cube.{bin,mem,idf,idf_mem}`, `fuga_talk_gpu.fuga`, `fuga_full_byte_tm.*`.
- **Удалено**: старые серии mirror (500–2500/phase/v1/trainonly/hybrid), `crystal_old/merged/v4`, `htm_src/htm_src_w/htm_tokio`, omni-варианты (1024/3d8/best/repos/v2/v3), sweep-кубы, `w_base.bin`, `fuga_5.gguf`, sidecar-бэкапы; **`train_checkpoints/` целиком (30G** — подтверждено бит-в-бит дубль корня, 16MB-хэши), **`target/debug` (13G)**, `test.1.er` (188M), `fuga_new_files.zip`, `src/bin/{chat.html,fuga_nano_mem.bin}`, `cube-3d.html`.
- Безопасно не тронуто: `target/release` (сборка для обучения), корпуса `.jsonl`, `workspace/`, `temp_repos/`, `necli/` (нужны кодам).
- Манифесты чистки: `/home/slava/.hermes/tmp/fuga_cleanup/` (manifest_phase1.txt, manifest_train_checkpoints.txt, AGENTS.md.bak).

## Ревизия стека под байтовый стандарт (09.08, вечер)
- **Удалена мёртвая ветка** (0 внешних использований, только токен-модели вне байтового стандарта): `src/ai/{agent,autonomous_mind,unity_mind,unified_mind,state_loader,mentalese,predictive_coder}.rs`, `src/bin/autonomous_cycle.rs` (+ их pub-use из mod.rs/lib.rs). Полная пересборка `cargo check --release --lib --bins` — прошла (только warnings).
- **Оставшееся деление**: байтовый/латентный стандарт (live): `sdr.rs` (byte_basis), `latent_jepa.rs`, `htm_temporal.rs`, `tm_generate.rs`, `kan.rs`, `hybrid.rs`, `hopfield.rs`, `byte_lstm.rs`, `gpu_ops.rs`, `soft_sdr.rs`; сервис-слой (core/memory_store/moe/…, используется omni-web/tgbot) — остаётся, т.к. подключает прод-память; легаси-токенные (self_mirror/temporal_predictor/hierarchical_jepa, text-dir) — вне стандарта, используются старыми стендами (omni-web fallback), требует отдельного решения — см. next steps.### Диагноз убийств прогонов (exit=143) — ОКОНЧАТЕЛЬНЫЙ (09.08)
- НЕ OOM, НЕ ребут: journalctl показывает `PrepareForSleep(true)` от
  org_kde_powerdevil — ноутбук уходит в suspend (крышка/таймаут), процесс
  получает SIGTERM. RAM была в норме (2722MB free), dmesg без OOM-kill.
- ЛЕЧЕНИЕ: длинные прогоны запускать через
  `systemd-inhibit --what=sleep ./target/release/hybrid_train ...` —
  держит inhibitor, PowerDevil не усыпит машину. Плюс --ckpt-every.
